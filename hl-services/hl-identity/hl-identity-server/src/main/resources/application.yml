server:
  port: 8090
  servlet:
    context-path: /identity
spring:
  application:
    name: hl-identity
  profiles:
    active: ${HL_ENV:dev}
#  jpa:
#    show-sql: true
#    hibernate:
#      ddl-auto: none
#    properties:
#      hibernate.format_sql: true
#    open-in-view: false
  datasource:
    url: ${DB_CONN:jdbc:mysql://127.0.0.1:13306/hl_identity?autoReconnect=true}
    username: root
    password: qwe!P4ss
    # 使用druid数据源
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    filters: stat
    maxActive: 20
    initialSize: 1
    maxWait: 60000
    minIdle: 1
    timeBetweenEvictionRunsMillis: 60000
    minEvictableIdleTimeMillis: 300000
    validationQuery: select 'x'
    testWhileIdle: true
    testOnBorrow: false
    testOnReturn: false
    poolPreparedStatements: true
    maxOpenPreparedStatements: 20
eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_SERVICE_URL:http://service1:8000/eureka}

swagger:
  title: hl-identity
  description: 提供用户登录、身份认证与授权相关接口
  version: v1.0.0
  enable: true
  controller-package-name: com.liuhll.hl.identity.controller

auth:
  whitelist:
    - /v1/login
#    - /organization/v1/api/org

jwt:
  secret: qY9Z3TMrSoqiolNQcgTYkkvCzN62ArNHL3XgtDww86X9WZGO6cAo7cz3yQX0qeJj
  expiration: 1000000
  header: Authorization
